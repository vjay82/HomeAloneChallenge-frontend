import { ParseDurationPipe } from './parse-duration.pipe';

describe('ParseDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
